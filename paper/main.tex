% RAID 2026 Submission — Springer LNCS format
% 20 pages excluding references
\documentclass[runningheads]{llncs}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc}

% Reduce spacing around floats (tables and figures)
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{2pt}

% Toggle: set to true for submission (anonymous), false for camera-ready
\newif\ifanonymous
\anonymoustrue  % Change to \anonymousfalse for camera-ready

\renewcommand{\thefootnote}{}

\begin{document}

\title{Evolutionary Weightless Neural Networks\\for Network Intrusion Detection}

\ifanonymous
  \author{Anonymous}
  \institute{Anonymous Institution}
\else
  \author{Luiz Alberto Crispiniano Garcia}
  \institute{Independent Researcher \\ \email{lacg@example.com}}
\fi

\maketitle

% ============================================================================
\begin{abstract}
Weightless Neural Networks (WNNs) based on Random Access Memory (RAM) neurons
offer constant-time inference with sub-microsecond lookup, making them
attractive for edge-deployed intrusion detection systems. However, their
classification performance depends on \emph{connectivity},
which input bits each neuron observes, a problem traditionally solved by random
initialization. We present an evolutionary optimization framework that
optimizes neuron count through grid search and genetic algorithm with
each neuron's random connectivity effectively performing feature selection
intrinsic to the architecture. The trained memory (100\,KB for our best deployable genome) fits
within the LUT fabric of a \$20 Zynq~Z-7020 FPGA, enabling $O(1)$
direct-lookup inference per neuron. We evaluate on three standard IDS
benchmarks (UNSW-NB15, CICIDS2017, CIC-IoT-2023) using temporal
and/or random splits with a seven-mode threshold calibration protocol
that exposes the sensitivity of binary classifiers to decision boundary
selection. Across up to 120 independent runs per configuration, our
approach achieves 87.02\% F1 on UNSW-NB15 temporal with a deployable
train-calibrated threshold (outperforming Random Forest by 0.61 points
at half the FPR) and 99.28\% F1 on CICIDS2017 random
(0.24\% false positive rate).
On the full 46.7M-record CIC-IoT-2023 benchmark, our approach spans a
four-order-of-magnitude memory frontier from a 20-byte microcontroller
detector to a hundred-megabyte sparse peak: a 25\,KB
400-neuron 8-bit configuration achieves 83.13\% F1 at 2.06\% FPR
(beating the Perceptron baseline by 2.08 points and fitting within
the BRAM of the smallest commercial FPGAs), while a peak 245-neuron
32-bit configuration reaches 84.79\% F1 at 1.59\% FPR. Vivado-verified
FPGA synthesis requires as few as 82 LUTs on a Zynq~Z-7020.

\keywords{Intrusion Detection \and Weightless Neural Networks \and
Evolutionary Optimization \and Feature Selection \and FPGA}
\end{abstract}

% ============================================================================
\section{Introduction}
\label{sec:intro}

% Hook: IDS at edge needs speed + small models
Network intrusion detection systems (NIDS) face a fundamental tension between
detection accuracy and deployment constraints. Deep learning models achieve
high accuracy but require megabytes of parameters, GPU hardware, and
millisecond-scale inference per sample~\cite{mdpi2024enhanced} ---
orders of magnitude slower than the ${\sim}$80\,ns per-packet budget
at 100\,Gbps line rate, making them impractical for inline packet
classification on edge devices. Conversely, lightweight rule-based
systems operate at wire speed but lack the adaptability to detect novel
attack patterns.

% Gap: WNNs exist for IDS but limited evaluation
Weightless Neural Networks (WNNs)~\cite{aleksander1984wisard,ludermir1999weightless} offer a
compelling middle ground: RAM-based neurons perform classification via direct
memory lookup in $O(1)$ time with model sizes measured in bytes rather than
megabytes. Recent work~\cite{susskind2023fwiw} demonstrated WNN-based IDS
achieving 98\% accuracy on UNSW-NB15 using random train/test splits, which
show stronger performance than temporal splits due to shared distributional
characteristics between training and testing data. Additionally, neuron
connectivity (which input bits each neuron observes) plays a critical role
in WNN architectures but is traditionally generated randomly, where network
quality depends on the initial assignment.

% Our contribution
Our work improves on two fronts:
\begin{itemize}
\item \emph{How the network is derived.} We introduce an optimization
  framework using genetic algorithm to discover optimal neuron
  counts. This optimization effectively performs automated feature selection
  intrinsic to the WNN architecture.
\item \emph{How the network is evaluated.} We test the resulting networks
  against unseen data using temporal or random splits across three datasets
  with a threshold calibration study that quantifies the gap
  between train-time and deployment-time performance.
\end{itemize}

% Contributions list
Our contributions are:
\begin{enumerate}
\item \textbf{Evolved connectivity as feature selection.} Optimizing
  network parameters discovers which input features matter without
  external feature selection with connectivity optimization providing
  the largest single improvement in classification performance.
\item \textbf{Compact architecture with sub-microsecond inference.}
  Our evolved WNN outperforms Random Forest and XGBoost on the UNSW-NB15
  temporal split and achieves 5.8$\times$ lower FPR than RF on CIC-IoT-2023,
  in a 100\,KB model deployable on a \$20 FPGA.
\item \textbf{Seven-mode threshold calibration study.} We identify threshold
  selection as an important factor in IDS evaluation and show that
  fitness-aligned threshold calibration reduces the temporal transfer
  gap from 5.43 to 0.33 percentage points on UNSW-NB15.
\item \textbf{Three-dataset evaluation with FPGA synthesis.} Results on
  UNSW-NB15 (temporal and random), CICIDS2017 (random), and CIC-IoT-2023
  (random, including a single-genome run on the full 46.7M-record
  benchmark) demonstrate architecture generalizability, with
  Vivado-verified FPGA deployment on a Zynq~Z-7020 for all three
  datasets.
\end{enumerate}

% ============================================================================
\section{Background}
\label{sec:background}

\subsection{RAM Neurons and Weightless Neural Networks}

% RAM neuron basics
Unlike conventional neural networks that learn continuous weights through
gradient descent, Weightless Neural Networks (WNNs)~\cite{aleksander1984wisard}
use Random Access Memory (RAM) neurons that perform classification via
direct memory lookup. A RAM neuron consists of $2^n$ memory cells, where
$n$ is the number of observed input bits, the neuron's \emph{address width}.
Given a binary input vector of length $d$, the neuron selects $n$ bits
through a fixed \emph{connectivity map} $C = \{c_1, \ldots, c_n\}$ where
$c_i \in \{0, \ldots, d{-}1\}$, concatenates them to form an $n$-bit
address, and returns the stored value at that address. Training writes
target labels to addressed cells; inference reads them in $O(1)$ time
with no multiply-accumulate operations.

% Generalization through partial connectivity
The fundamental mechanism enabling WNN generalization is
\emph{partial connectivity}~\cite{aleksander1984wisard,ludermir1999weightless}. A fully-connected neuron ($n = d$) memorizes
every training example without generalizing, since each input maps to a
unique address. With partial connectivity ($n \ll d$), many distinct inputs
share the same address (those that agree on the $n$ selected bit
positions), causing the neuron to learn a \emph{feature pattern} over its
observed bits. An ensemble of $N$ neurons with diverse connectivity maps
provides $N$ complementary views of the input, and their aggregated scores
produce a classification decision. We refer to the neuron count~$N$,
address width~$n$, and connectivity maps~$C$ collectively as the
\emph{network parameters}.

% QUAD_WEIGHTED memory
Traditional RAM neurons store binary values (TRUE/FALSE), while
Probabilistic Logic Nodes (PLN) add an
\emph{undefined} state ($u$) for untrained cells, and Multi-valued PLN
(MPLN)~\cite{ludermir1999weightless} generalize to finer granularity (e.g., 11 levels from 0.0 to 1.0
in 0.1 steps, starting at 0.5 for undefined).

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  cell/.style={draw, minimum size=9mm, inner sep=0pt, font=\footnotesize},
  neuron/.style={draw, rounded corners, fill=blue!8, minimum width=28mm,
                 minimum height=14mm, font=\small\bfseries},
  conn/.style={-{Stealth[length=2mm]}, thick},
  memcell/.style={draw, minimum size=7mm, inner sep=0pt, font=\scriptsize},
]

% Input grid (3x3) — wider spacing (1.2) for arrow routing
\node[font=\small\bfseries] at (1.2, 3.6) {Input vector};
\foreach \i/\val/\clr in {0/1/green!25, 1/0/red!15, 2/1/green!25,
  3/1/green!25, 4/0/red!15, 5/0/red!15, 6/1/green!25, 7/1/green!25, 8/0/red!15} {
  \pgfmathtruncatemacro{\col}{mod(\i,3)}
  \pgfmathtruncatemacro{\row}{2-div(\i,3)}
  \node[cell, fill=\clr] (in\i)
    at (\col*1.2, \row*1.2) {};
  % Value in center-right area
  \node[font=\footnotesize, anchor=center] at ([shift={(0.5mm,-0.5mm)}]in\i.center) {\val};
  % Label in top-left corner, tight
  \node[font=\tiny, gray, anchor=north west, inner sep=0.5pt] at (in\i.north west) {$b_{\i}$};
}

% Neuron A (selects bits 0, 3, 6) — larger box
\node[neuron] (nA) at (6.0, 2.4) {Neuron A};
\node[font=\tiny, gray] at (6.0, 1.5) {$C_A = \{0, 3, 6\}$};

% Neuron B (selects bits 2, 4, 7) — larger box
\node[neuron] (nB) at (6.0, -0.4) {Neuron B};
\node[font=\tiny, gray] at (6.0, -1.3) {$C_B = \{2, 4, 7\}$};

% === Connections to Neuron A (blue): bits 0, 3, 6 ===
% b0: straight up, straight right, straight down to neuron level, straight right into Neuron A
\draw[conn, blue!70] (in0.north) -- (0, 3)
  -- (3.4, 3) -- (3.4, 2.7) -- ([yshift=3mm]nA.west);

% b3 (middle-left, 0,1.2): go up between row 0 and row 1, right through gap, up to neuron level
\draw[conn, blue!70] (in3.north) -- (0, 1.8)
  -- (3.4, 1.8) -- (3.4, 2.4) -- (nA.west);

% b6 (bottom-left, 0,0): go DOWN below grid, right, up to neuron level, right into Neuron A
\draw[conn, blue!70] (in6.south) -- (0, -0.9)
  -- (3.6, -0.9) -- (3.6, 2.1) -- ([yshift=-3mm]nA.west);

% === Connections to Neuron B (orange): bits 2, 4, 7 ===
% b2 (top-right, 2.4,2.4): go right, then down to Neuron B top
\draw[conn, orange!80] (in2.east) -- ++(0.3, 0) |- ([yshift=3mm]nB.west);

% b4 (middle-center, 1.2,1.2): go right past grid, then to Neuron B
\draw[conn, orange!80] (in4.south) -- (1.2, 0.6)
  -- (3, 0.6) |- (nB.west);

% b7 (bottom-center, 1.2,0): go down to Neuron B level, then straight right
\draw[conn, orange!80] (in7.south) -- (1.2, -0.7)
  -- (3.8, -0.7) -- ([yshift=-3mm]nB.west);

% Address labels
\node[font=\tiny] at (6.0, 3.3) {addr: $b_0 b_3 b_6 = 111_2 = 7$};
\node[font=\tiny] at (6.0, -1.6) {addr: $b_2 b_4 b_7 = 101_2 = 5$};

% Memory table A
\node[font=\scriptsize\bfseries] at (9.8, 3.1) {Memory A};
\foreach \a/\val/\c in {0/0.25/gray!20, 1/0.25/gray!20, 2/0.75/blue!15,
  3/0.25/gray!20, 4/0.00/red!15, 5/1.00/green!25, 6/0.75/blue!15,
  7/1.00/green!25} {
  \pgfmathtruncatemacro{\col}{mod(\a,4)}
  \pgfmathtruncatemacro{\row}{1-div(\a,4)}
  \node[memcell, fill=\c] (mA\a) at (8.7+\col*0.65, 1.8+\row*0.65) {};
  \node[font=\scriptsize, anchor=center] at ([shift={(0mm,-0.5mm)}]mA\a.center) {\val};
  \node[font=\tiny, gray, anchor=north west, inner sep=0.5pt] at (mA\a.north west) {\a};
}
\draw[-{Stealth[length=2mm]}, thick, green!60!black] (nA.east) -- (7.8, 2.4) -- (7.8, 1.2) -- (10.65, 1.2) -- (mA7.south);

% Memory table B
\node[font=\scriptsize\bfseries] at (9.8, 0.7) {Memory B};
\foreach \a/\val/\c in {0/0.25/gray!20, 1/0.00/red!15, 2/0.25/gray!20,
  3/0.75/blue!15, 4/1.00/green!25, 5/0.75/blue!15, 6/0.25/gray!20,
  7/0.00/red!15} {
  \pgfmathtruncatemacro{\col}{mod(\a,4)}
  \pgfmathtruncatemacro{\row}{1-div(\a,4)}
  \node[memcell, fill=\c] (mB\a) at (8.7+\col*0.65, -0.6+\row*0.65) {};
  \node[font=\scriptsize, anchor=center] at ([shift={(0mm,-0.5mm)}]mB\a.center) {\val};
  \node[font=\tiny, gray, anchor=north west, inner sep=0.5pt] at (mB\a.north west) {\a};
}
\draw[-{Stealth[length=2mm]}, thick, blue!60] (nB.east) -- (7.8, -0.4) -- (7.8, -1.2) -- (9.25, -1.2) -- ([xshift=-1mm]mB5.south);

% Output
\node[draw, rounded corners, fill=yellow!15, minimum width=20mm,
      font=\small] (out) at (9.8, -2.0)
  {$\frac{1.0 + 0.75}{2} = 0.875$};
\node[font=\tiny, gray] at (9.8, -2.7) {score $\geq \tau \Rightarrow$ \textbf{attack}};

\draw[-{Stealth[length=2mm]}, thick] (mA7.east) -- (11.4, 1.8) -- (11.4, -1.3) -- (10.2, -1.3) -- ([xshift=4mm]out.north);
\draw[-{Stealth[length=2mm]}, thick] ([xshift=1mm]mB5.south) -- ([xshift=-3.5mm]out.north);

\end{tikzpicture}
\caption{A \emph{Quad-State RAM} (QSR) neuron architecture with two neurons
($N{=}2$, $n{=}3$ address bits each). Each neuron selects 3 bits from the
9-bit input via its connectivity map, forms a 3-bit address, and looks up
its QSR memory ($2^3{=}8$ cells). Cell colors:
\textcolor{green!60!black}{TRUE (1.0)},
\textcolor{blue!60}{WEAK\_TRUE (0.75)},
\textcolor{gray}{WEAK\_FALSE (0.25)},
\textcolor{red!70}{FALSE (0.0)}.
The mean score is thresholded to classify the input.}
\label{fig:ram-neuron}
\end{figure}

We introduce \emph{Quad-State RAM} (QSR) memory (Figure~\ref{fig:ram-neuron})
with four states encoded in just 2~bits: FALSE~($0.0$),
WEAK\_FALSE~($0.25$), WEAK\_TRUE~($0.75$), and TRUE~($1.0$). Unlike PLN's symmetric undefined state~($0.5$), QSR
cells default to WEAK\_FALSE~($0.25$), biasing untrained neurons toward
the majority class (normal traffic). When a cell trained as TRUE is
subsequently addressed with a normal-class example, it transitions to
WEAK\_TRUE, preserving partial confidence
from both classes. This provides smoother probability estimates than
binary voting while requiring only 2~bits per cell, matching PLN's
compactness with richer representation.

\subsection{Thermometer Encoding}

WNNs require binary inputs, but network flow features (packet counts,
byte rates, inter-arrival times) are continuous, usually encoded as
float64. We convert each feature
to a binary vector using \emph{thermometer encoding}~\cite{susskind2022bthowen}.
Given a feature value $x$ and $k$ thresholds $\theta_1 < \cdots < \theta_k$,
the encoding produces $k$ bits: bit $i$ is 1 if $x \geq \theta_i$, else 0.
This preserves ordinal relationships: similar values differ by few bits,
enabling RAM neurons to generalize across nearby feature values.
This ordinal encoding also provides natural noise robustness: a small
measurement error shifts at most one threshold boundary, flipping a
single bit in the encoded vector.

We compute thresholds using the \emph{distributive} (quantile-based) method,
which places thresholds at equally-spaced quantiles of the training
distribution. This is robust to skewed distributions common in network
traffic (e.g., packet sizes follow a heavy-tailed distribution). With $k$
bits per feature and a feature set $F$, the total input width is
$d = k \times |F|$ bits.

\subsection{Evaluation Datasets}

We evaluate on three standard IDS benchmarks, each with distinct
characteristics:

\textbf{UNSW-NB15}~\cite{moustafa2015unswnb15} contains network flow records
from the Australian Centre for Cyber Security with 42 numeric features and 10 attack
categories. The standard temporal split (175K train / 82K test) separates
training and testing periods, preventing data leakage. We also evaluate on a
deduplicated random 80/20 split (1.27M / 317K) for comparison with prior
work~\cite{susskind2023fwiw}. Both splits are published with
top-20 feature rankings\ifanonymous\footnote{Dataset URLs anonymized
for review; will be included in camera-ready.}\else~\cite{hf_unsw}\fi.

\textbf{CICIDS2017}~\cite{sharafaldin2018cicids2017} comprises five days of
network traffic captured at the Canadian Institute for Cybersecurity with
78 flow-level features and 14 attack types. We use a stratified 80/20
random split (2.3M train / 566K test)\ifanonymous\else~\cite{hf_cicids}\fi,
consistent with the majority of published work on this dataset.

\textbf{CIC-IoT-2023}~\cite{neto2023ciciot} captures IoT traffic from 105
heterogeneous devices with 33 attack types across 7 classes. With 46.7M
records heavily skewed toward attacks (97.6\%), we subsample to 1.3M records
for a manageable benchmark, as a random split (no temporal ordering in the
source data). Both the 1.3M subsample and full 46M dataset are
published\ifanonymous\else~\cite{hf_ciciot}\fi.

% ============================================================================
\section{Related Work}
\label{sec:related}

\subsection{WNN-Based Intrusion Detection}

The WiSARD~\cite{ludermir1999weightless} architecture pioneered RAM-based pattern
recognition, and several works have applied WNNs to network security.
Santiago et al.~\cite{santiago2020fpga} demonstrated WNN-based anomaly
detection with random connectivity.

The most directly related work is FWIW~\cite{susskind2023fwiw}, which
achieves 98\% accuracy on UNSW-NB15 with a 272-byte model deployed on FPGA.
FWIW uses Bloom filter-based neurons with H3 hashing and backpropagation
training with random connectivity and one threshold mode. Our work
differs in three ways: (1)~we evaluate on both random and temporal splits,
as random splits may allow some data leakage between train and test sets;
(2)~we use random connectivity as a starting point and apply a genetic
algorithm to optimize neuron count; and (3)~we evaluate seven threshold
modes to identify which best predicts deployment performance.

BTHOWeN~\cite{susskind2022bthowen} introduced thermometer encoding with
learned thresholds for WNN classification, achieving strong results on
tabular benchmarks. We adopt their encoding scheme but replace learned
thresholds with distributive (quantile-based) placement, which sets bin
boundaries at equal-probability intervals of the training distribution.
This avoids over-representing dense regions of noisy features (e.g.,
inter-arrival times with heavy tails) and ensures each thermometer bit
carries roughly equal information content, improving robustness without
requiring per-feature threshold optimization.

\subsection{Machine Learning for IDS}

Traditional ML approaches (Random Forest, XGBoost, SVM) achieve
85--90\% F1 on UNSW-NB15's temporal split~\cite{zoghi2024ids} but require
models of 10--50\,MB. Deep learning approaches (BiLSTM~\cite{mdpi2024enhanced},
CNN-LSTM) push accuracy higher at the cost of microsecond-scale GPU
inference and 50--300\,W power envelopes, limiting deployment on
edge devices with single-digit watt budgets.

A critical methodological issue pervades the IDS literature: most published
results use random train/test splits, reporting 97--99\% accuracy that does
not reflect deployment performance. Zoghi and Serpen~\cite{zoghi2024ids} showed
that the same models achieving 99\% on random splits drop to 87\% on the
standard temporal split, a finding consistent with our evaluation. For
CICIDS2017, the situation is similar: near-perfect random-split results
contrast with the absence of temporal evaluation in the literature. Known
labeling errors~\cite{engelen2021cicids} further complicate comparisons.

\subsection{FPGA-Based Network Security}

FPGA acceleration for network security spans from packet-level inspection
(Pigasus~\cite{zhao2020pigasus}) to ML inference
(LogicNets~\cite{umuroglu2020logicnets}, PolyLUT~\cite{andronic2023polylut}).
WNNs are particularly well-suited for FPGA deployment because RAM neurons
map directly to FPGA lookup tables (LUTs) and block RAM (BRAM), requiring
no multiply-accumulate units. FWIW~\cite{susskind2023fwiw} demonstrated
this with sub-microsecond inference on a Virtex UltraScale+. Our work
extends this direction with evolved connectivity that can further reduce
resource utilization by discovering sparser, more efficient neuron
configurations.

% ============================================================================
\section{Architecture}
\label{sec:architecture}

\subsection{Single-Cluster Binary Discriminator}

Our IDS uses a \emph{single-cluster discriminator}: $N$ QSR
neurons, each with $b$ address bits, observing the same thermometer-encoded
input vector $\mathbf{x} \in \{0,1\}^d$. Each neuron $i$ has a connectivity
map $C_i = \{c_1^i, \ldots, c_b^i\}$ that selects $b$ bits from
$\mathbf{x}$ to form an address. The neuron outputs the value stored at
that address in its $2^b$-cell memory.

Training iterates over labeled examples: for each flow, each QSR neuron
computes its address and writes the target label (0 for normal, 1 for
attack) to the addressed cell using the QSR update rule
(conflicting writes produce weak states, while confirming writes
produce strong states).

At inference, the discriminator computes the mean score across all $N$
neurons and applies a threshold $\tau$ to produce a binary classification:
\begin{equation}
\hat{y} = \begin{cases}
  1 \text{ (attack)} & \text{if } \frac{1}{N}\sum_{i=1}^{N} \text{mem}_i[\text{addr}_i(\mathbf{x})] \geq \tau \\
  0 \text{ (normal)} & \text{otherwise}
\end{cases}
\end{equation}

This architecture is deliberately simple, a single cluster with one
threshold, making the connectivity optimization problem tractable while
still achieving competitive accuracy. The key insight is that
\emph{classification quality depends primarily on connectivity}, not on
architectural complexity.

\subsection{Phased Optimization Pipeline}

We optimize the discriminator's network parameters $(N, b, C)$ through
a two-phase pipeline, where the output population of the first phase
seeds the second.

\textbf{Phase 1: Grid Search.} Evaluation of $(N, b)$
configurations (e.g., $N \in \{5, 10, \ldots, 500\}$,
$b \in \{4, 8, \ldots, 34\}$) with random connectivity. Each configuration
is trained on the full training set and evaluated using 5-fold
cross-validation, then ranked by a fitness function.  The best $(N, b)$
configuration is used to generate an initial population of $P$ genomes
(default $P{=}50$), each with the same architecture but different random
connectivity, seeding the subsequent GA phase.

\noindent\textbf{Phase 2: GA Neurons.} A genetic algorithm optimizes neuron count
$N$ starting from the grid search population.  Elitism preserves the top
20\% of genomes each generation.  Crossover exchanges neuron groups
between parents; mutation adds or removes neurons.  The GA preserves
each neuron's learned connections and trained memory, so structural
changes (adding/removing neurons) do not discard prior training.
This phase discovers the optimal model capacity for the dataset,
typically reducing neuron count by 40--43\% while maintaining or
improving accuracy (Section~\ref{sec:phase-progression}).

\textbf{Fitness function.}
IDS metrics (F1, FPR, cross-entropy) differ in scale, making weighted
sums sensitive to normalization. Both phases rank genomes per metric
within the population, then combine ranks via a weighted harmonic mean:
\begin{equation}
\text{fitness}(g) = \frac{w_{CE} + w_{F1} + w_{FPR}}{%
  \frac{w_{CE}}{r_{CE}(g)} + \frac{w_{F1}}{r_{F1}(g)} + \frac{w_{FPR}}{r_{FPR}(g)}}
\label{eq:fitness}
\end{equation}
where $r_m(g)$ is genome $g$'s rank for metric $m$ (lower rank = better).
The CICIDS2017 runs use $w_{CE}{=}0.2$, $w_{F1}{=}0.3$,
$w_{FPR}{=}0.4$, $w_{Acc}{=}0.1$.
For UNSW-NB15 temporal and CIC-IoT-2023, we adopt refined weights
$w_{CE}{=}0.1$, $w_{F1}{=}0.35$, $w_{FPR}{=}0.35$, $w_{Acc}{=}0.2$,
balancing F1 and FPR equally (Section~\ref{sec:results-ciot}). The
refinement was driven by empirical observation that the original
FPR-heavy weights caused the GA to over-optimize for low FPR at the
expense of F1; equal F1/FPR weighting produces genomes with a better
balanced operating point across threshold modes. On UNSW-NB15 temporal,
this change reduced the train\_cal--val\_cal gap from 5.43\,pp to
0.23\,pp (Section~\ref{sec:results-unsw}).

\subsection{GPU-Accelerated Optimization}

Population-based optimization requires evaluating hundreds of genome
configurations per generation across hundreds of generations.
We implement a Rust accelerator with Apple Metal GPU support that
parallelizes both training (writing target labels to neuron memories)
and evaluation (computing scores across all test examples).

The accelerator computes neuron addresses via GPU kernel dispatch,
achieving ${\sim}$100M address computations per second on an
M4~Max GPU\@.  A complete two-phase run (grid search + GA~neurons)
completes in 5~minutes on average per seed on UNSW-NB15, enabling
100+ statistically independent runs.

% ============================================================================
\section{Evaluation Methodology}
\label{sec:methodology}

\subsection{Temporal and Random Evaluation Protocols}

Evaluation protocol is a critical factor in IDS benchmarking. Random
train/test splits allow training and test sets to share temporal
characteristics (and, in datasets with duplicate records, near-identical
flows), which can lead to higher reported accuracy. Temporal splits,
where training data precedes test data chronologically, better simulate
deployment conditions where the model must generalize to future traffic
patterns.

We evaluate under the temporal protocol for UNSW-NB15 and random splits
for UNSW-NB15, CICIDS2017, and CIC-IoT-2023. All splits are published
on HuggingFace\ifanonymous\else~\cite{hf_unsw,hf_cicids,hf_ciciot}\fi{}
for reproducibility:
\begin{itemize}
\item \textbf{Temporal split (UNSW-NB15):} The official 175K/82K
  split with temporal separation between training and testing periods.
\item \textbf{Random splits (all datasets):} Stratified random 80/20 splits:
  UNSW-NB15 (1.27M/317K),
  CICIDS2017 (2.3M/566K), CIC-IoT-2023 (1.07M/268K).
\end{itemize}

\subsection{Seven-Mode Threshold Calibration}

Binary IDS classifiers produce a continuous score that must be
thresholded to yield a classification. The choice of threshold
dramatically affects F1 and FPR, yet most published work reports
results under a single (a fixed 0.5, or sometimes unspecified) threshold mode. We evaluate
every genome under seven calibration strategies to expose this
sensitivity:

\begin{enumerate}
\item \textbf{Train-calibrated (train\_cal):} Threshold swept on training-set
  scores to maximize the same fitness function used by the GA.
\item \textbf{Fixed 0.5:} No calibration; the midpoint threshold
  (distribution-agnostic baseline).
\item \textbf{Platt scaling}~\cite{platt1999probabilistic}: Logistic
  regression on held-out scores, $P(\text{attack})\!=\!\sigma(as\!+\!b)$.
\item \textbf{Beta calibration}~\cite{kull2017beta}: Three-parameter
  logit model, more flexible than Platt for asymmetric distributions.
\item \textbf{Empirical:} Histogram-based threshold from held-out
  score distribution.
\item \textbf{Empirical cumulative:} CDF-based threshold selection.
\item \textbf{Validation (val\_cal):} Threshold swept on the held-out validation set
  (not used during training or GA selection). This represents an upper
  bound on achievable performance, not available in deployment, but
  quantifies the calibration gap.
\end{enumerate}

The gap between validation and train-calibrated performance
quantifies how much accuracy is ``left on the table'' by
imperfect threshold selection, a finding we report for each
dataset and configuration.

\textbf{Fitness-aligned threshold optimization.}  The train\_cal mode
sweeps the threshold to maximize the \emph{same} weighted-fitness
function (Equation~\ref{eq:fitness}) that the GA uses for genome
selection, rather than maximizing F1 alone.  This alignment is
methodologically critical: when the threshold sweep optimizes F1 but
the GA selects on a fitness combining CE, F1, FPR, and accuracy, the
GA receives misleading FPR signals and discards genomes that would
have been strong under the actual fitness criterion.  The
co-optimization ensures that architecture search and threshold
selection work together rather than at cross purposes.

\subsection{Statistical Protocol}

Each configuration is run as at least 100 independent flows with
different seeds (101 for UNSW-NB15 temporal, 120 for CICIDS2017,
112 for CIC-IoT-2023). We report mean $\pm$ standard deviation.
With $n \geq 100$ and typical $\sigma \approx 2\%$, the 95\% confidence
interval for the mean is $\pm 0.39\%$, sufficient to distinguish
meaningful differences between configurations.

K-fold cross-validation ($k{=}5$) is applied \emph{every generation}
during evolution within both phases: the training set is partitioned
into 5 folds, and each genome is trained and evaluated on all 5
train/validation splits every generation. The averaged fitness across
all 5 folds determines selection, preventing the GA from overfitting
to a particular data partition and providing more robust genome ranking.
Early stopping terminates evolution when the best K-fold fitness does
not improve by at least $0.05\%$ for 5 consecutive check intervals
(every 10 generations), avoiding unnecessary computation and further
guarding against overfitting.

All reported results use the \emph{best-fitness genome from the
final optimization phase}, evaluated by the full-dataset evaluator
(175K train $\to$ 82K test for UNSW-NB15) with all seven threshold
modes. Each flow contributes one data point to the aggregate
statistics, ensuring that reported means reflect the full
distribution of outcomes.

% ============================================================================
\section{Experimental Results}
\label{sec:results}

\subsection{Dataset Results}

\subsubsection{UNSW-NB15 Temporal Results.}
\label{sec:results-unsw}

Table~\ref{tab:unsw} reports results from 101 independent runs on the
UNSW-NB15 temporal split (8-bit thermometer, top-20 RF features,
population size~50) using balanced fitness weights
($w_{F1}{=}0.35$, $w_{FPR}{=}0.35$, $w_{CE}{=}0.1$, $w_{Acc}{=}0.2$)
and fitness-aligned threshold optimization
(Section~\ref{sec:methodology}).

The train-calibrated threshold achieves 87.02\% $\pm$ 2.58\% F1-macro
with 13.76\% FPR --- outperforming Random Forest (86.41\% F1,
25.22\% FPR) by 0.61\,pp F1 at half the FPR.  The validation
threshold reaches 87.34\% $\pm$ 2.12\% F1 at 5.43\% FPR, a gap of
only 0.33\,pp from train\_cal.

\begin{table}[ht!]
\centering
\caption{UNSW-NB15 temporal split results (101 runs, 8-bit thermometer,
GA~Neurons best-fitness genome, balanced fitness weights).
Neurons: 333 $\pm$ 128, address bits: 31.
Full seven-mode breakdown in Appendix~\ref{app:thresholds}.}
\label{tab:unsw}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrr}
\toprule
Threshold Mode & F1-macro (\%) & FPR (\%) & Accuracy (\%) \\
\midrule
Best F1$^\ddagger$         & \textbf{90.52}   & 4.13               & \textbf{90.54} \\
Best FPR$^\ddagger$        & 71.80             & \textbf{0.00}      & 72.64 \\
\midrule
Train-cal                  & 87.02 $\pm$ 2.58 & 13.76 $\pm$ 4.63   & 87.16 $\pm$ 2.53 \\
Fixed 0.5                  & 83.99 $\pm$ 7.26 & 17.99 $\pm$ 15.15  & 84.68 $\pm$ 6.17 \\
Validation (val\_cal)      & 87.34 $\pm$ 2.12 & 5.43 $\pm$ 4.02    & 87.37 $\pm$ 2.10 \\
\bottomrule
\multicolumn{4}{l}{\small $^\ddagger$Best single genome across all 101 seeds, genome types, and thresholds.}
\end{tabular*}
\end{table}

\subsubsection{CICIDS2017 Random Results.}
\label{sec:results-cicids}

Table~\ref{tab:cicids} reports results from 120 runs on CICIDS2017 with
16-bit thermometer encoding (top-20 RF features, random 80/20 split,
population size~50) using the original FPR-weighted fitness
($w_{FPR}{=}0.4$, $w_{F1}{=}0.3$, $w_{CE}{=}0.2$, $w_{Acc}{=}0.1$).
The 16-bit encoding
was selected based on our thermometer sweep analysis
(Section~\ref{sec:discussion}), which identified 16~bits as the optimal
resolution for this dataset.

Performance is substantially higher than UNSW-NB15 temporal, reflecting
the easier random split.  The validation threshold achieves 99.30\%
$\pm$ 0.48\% F1-macro with 0.22\% FPR.  The train-calibrated threshold
nearly matches (99.28\% F1, 0.24\% FPR), confirming minimal calibration
difficulty on random splits.  Among individual genomes, the best F1
achieves 99.52\% F1 / 0.13\% FPR / 99.70\% Acc, within 0.22\,pp of
Random Forest (Table~\ref{tab:baselines}).

\begin{table}[ht!]
\centering
\caption{CICIDS2017 random split results (120 runs, 16-bit thermometer,
GA~Neurons best-fitness genome). Neurons: 198 $\pm$ 146, address bits: 32.
Full seven-mode breakdown in Appendix~\ref{app:thresholds}.}
\label{tab:cicids}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrr}
\toprule
Threshold Mode & F1-macro (\%) & FPR (\%) & Accuracy (\%) \\
\midrule
Best F1$^\ddagger$         & \textbf{99.55}   & 0.11              & \textbf{99.71} \\
Best FPR$^\ddagger$        & 78.90             & \textbf{0.03}     & 89.55 \\
\midrule
Train-cal                  & 99.28 $\pm$ 0.52 & 0.24 $\pm$ 0.27  & 99.55 $\pm$ 0.33 \\
Fixed 0.5                  & 99.09 $\pm$ 0.63 & 0.45 $\pm$ 0.29  & 99.42 $\pm$ 0.40 \\
Validation (val\_cal)      & 99.30 $\pm$ 0.48 & 0.22 $\pm$ 0.12  & 99.56 $\pm$ 0.30 \\
\bottomrule
\multicolumn{4}{l}{\small $^\ddagger$Best single genome across all 120 seeds, genome types, and thresholds.}
\end{tabular*}
\end{table}

\subsubsection{CIC-IoT-2023 Results.}
\label{sec:results-ciot}

We evaluate on CIC-IoT-2023 using two complementary protocols: a
112-run statistical batch on a 1.3M random subsample (matching the
CICIDS2017 methodology) and a single-genome evaluation on the full
46.7M-record dataset for direct comparison with prior published
baselines.

\paragraph{Methodology refinements (1.3M subsample, 112 runs).}
The CIC-IoT-2023 runs use the balanced fitness weights and
fitness-aligned threshold optimization described in
Sections~\ref{sec:architecture} and~\ref{sec:methodology}.

\begin{table}[ht!]
\centering
\caption{CIC-IoT-2023 1.3M subsample (112 runs, train\_cal threshold,
best-fitness genome). Phase progression and best individual genomes
across all seeds, genome types, and thresholds.}
\label{tab:ciot-phase}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrr}
\toprule
 & F1-macro (\%) & FPR (\%) & Accuracy (\%) \\
\midrule
Best F1$^\ddagger$         & \textbf{83.04}   & 20.18             & 90.62 \\
Best FPR$^\ddagger$        & 70.34             & \textbf{0.03}     & 76.93 \\
\midrule
Grid Search (train\_cal)   & 79.55 $\pm$ 0.43 & 4.51 $\pm$ 1.65  & 86.45 $\pm$ 0.56 \\
GA Neurons (train\_cal)    & 80.08 $\pm$ 0.48 & 4.26 $\pm$ 0.49  & 86.85 $\pm$ 0.42 \\
\midrule
$\Delta$ (GA$-$GS)        & +0.53            & $-$0.25           & +0.40 \\
\bottomrule
\multicolumn{4}{l}{\small $^\ddagger$Best single genome across all 112 seeds, genome types, and thresholds.}\\
\multicolumn{4}{l}{\small GA reduces neuron count by 19\% (397$\to$322), mirroring CICIDS2017 (Table~\ref{tab:phase}).}
\end{tabular*}
\end{table}

\paragraph{Full 46M dataset, Pareto frontier across memory tiers.}
To compare with prior work that reports on the full 46.7M-record
CIC-IoT-2023, we trained 13 single-genome configurations on the
complete dataset (30.8M train / 7.7M test, random 80/20), spanning
four orders of magnitude in memory footprint. Architectures were
selected from the 1.3M-subsample search and transferred to 46M
without further GA refinement, isolating the effect of training-data
volume from architecture-search effects. Table~\ref{tab:ciot-46m}
groups the results into three deployment tiers --- micro (20\,B to
2\,KB), small (5--25\,KB), and peak (sparse memory in the
hundred-megabyte range) --- and compares against the baselines
reported by Neto et al.~\cite{neto2023ciciot}.

\begin{table}[ht!]
\centering
\caption{Pareto frontier on the full 46M CIC-IoT-2023 (random 80/20).
Single-genome runs with no GA on 46M; architectures transferred from
the 1.3M subsample search. Baselines from Neto et al.~\cite{neto2023ciciot}.
``--'' indicates a metric not reported by the original work.}
\label{tab:ciot-46m}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrl}
\toprule
Tier & Method & F1 (\%) & FPR (\%) & Acc (\%) & Model Size \\
\midrule
\multirow{5}{*}{Micro}
 & Ours (5n $\times$ 4b)     & 66.66\,$\pm$\,6.36 & 8.84\,$\pm$\,12.30 & 90.68\,$\pm$\,4.49 & 20\,B \\
 & Ours (100n $\times$ 4b)   & 80.32 &  2.85 & 96.66 & 400\,B \\
 & Ours (200n $\times$ 4b)   & 79.37 &  2.38 & 96.38 & 800\,B \\
 & Ours (300n $\times$ 4b)   & 79.73 &  2.90 & 96.50 & 1.2\,KB \\
 & Ours (500n $\times$ 4b)   & 79.60 &  3.58 & 96.49 & 2.0\,KB \\
\midrule
\multirow{4}{*}{Small}
 & Ours (5n $\times$ 12b)    & 80.32 &  6.02 & 96.76 & 5.0\,KB \\
 & Ours (100n $\times$ 8b)   & 79.89 &  1.68 & 96.51 & 6.2\,KB \\
 & Ours (300n $\times$ 8b)   & 81.58 &  2.11 & 96.96 & 18.8\,KB \\
 & \textbf{Ours (400n $\times$ 8b)} & \textbf{83.13} & \textbf{2.06} & \textbf{97.33} & \textbf{25\,KB} \\
\midrule
\multirow{4}{*}{Peak}
 & Ours (96n $\times$ 32b)   & 84.36 &  1.61 & 97.59 & ${\sim}$200\,MB$^*$ \\
 & Ours (198n $\times$ 32b)  & 84.68 &  2.00 & 97.67 & ${\sim}$400\,MB$^*$ \\
 & Ours (245n $\times$ 32b)  & 84.79 &  1.59 & 97.68 & ${\sim}$500\,MB$^*$ \\
 & Ours (500n $\times$ 34b)  & 84.73 &  1.91 & 97.68 & ${\sim}$1\,GB$^*$ \\
\midrule
\multirow{3}{*}{\shortstack[l]{Baseline\\(46M)}}
 & RF (ours, 46M)   & 92.64 & 13.48 & 99.18 & ${\sim}$50\,MB \\
 & XGBoost (ours)    & 91.67 & 14.80 & 99.06 & ${\sim}$5\,MB \\
 & Perceptron        & 81.05 & ---   & 98.18 & ${\sim}$10\,KB \\
\midrule
\multirow{3}{*}{\shortstack[l]{Neto et al.$^\dag$\\(46M)}}
 & DNN              & 94.03$^\dag$ & ---  & 99.44 & ${\sim}$5\,MB \\
 & AdaBoost         & 95.63$^\dag$ & ---  & 99.59 & ${\sim}$10\,MB \\
 & Random Forest    & 96.53$^\dag$ & ---  & 99.68 & ${\sim}$50\,MB \\
\bottomrule
\multicolumn{6}{l}{\small All ``Ours'' rows: F1-macro, train\_cal threshold; 5n$\times$4b = mean$\pm$std (10 seeds), rest = single seed.}\\
\multicolumn{6}{l}{\small $^*$Sparse memory; thermometer collisions reduce actual entries far below $2^b$.}\\
\multicolumn{6}{l}{\small $^\dag$Neto et al.\ use StandardScaler + a different 80/20 split; F1 averaging method unspecified in the original paper.}
\end{tabular}}
\end{table}

\subsection{Phase Progression Analysis}
\label{sec:phase-progression}

Our two-phase pipeline first performs a grid search over neuron counts
and address widths, then refines the best genome with a genetic algorithm
that mutates neuron count while preserving evolved connections.
Table~\ref{tab:phase} compares the two phases on CICIDS2017.

The GA phase improves validation F1 by 0.05 percentage points on average
(99.35\% $\to$ 99.40\%) while reducing neuron count by 43\%
(340 $\to$ 194 neurons).  The GA also halves the F1 variance
($\pm$0.11\% $\to$ $\pm$0.06\%), indicating that neuron-count
optimization acts as a regularizer, pruning redundant neurons that
add noise without improving discrimination.

\begin{table}[ht!]
\centering
\caption{Phase progression on CICIDS2017 (120 runs, val\_cal threshold,
best-fitness genome).}
\label{tab:phase}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrrr}
\toprule
Phase & F1-macro (\%) & FPR (\%) & Neurons & Bits \\
\midrule
Grid Search    & 99.35 $\pm$ 0.07 & 0.212 $\pm$ 0.064 & 340 $\pm$ 115 & 34 (84\%) \\
GA Neurons     & 99.40 $\pm$ 0.06 & 0.196 $\pm$ 0.063 & 194 $\pm$ 143 & 32 (62\%) \\
\midrule
$\Delta$       & +0.05            & $-$0.016           & $-$146 ($-$43\%) & \\
\bottomrule
\end{tabular*}
\end{table}

\subsection{Comparison with Baselines}
\label{sec:baselines}

Table~\ref{tab:baselines} compares our WNN against conventional ML
baselines and the only other WNN-based IDS (FWIW).  All ``Ours''
entries use identical data splits; RF/XGBoost receive raw numeric
features (their natural format), while our WNN uses thermometer-encoded
binary features (its natural format).

On the UNSW-NB15 temporal split (101 runs, balanced fitness weights),
our WNN achieves 87.02\% F1 at 13.76\% FPR under the deployable
train\_cal threshold, outperforming RF (86.41\% F1, 25.22\% FPR) by
0.61\,pp F1 at nearly half the FPR.  The best individual genome
reaches \textbf{90.52\% F1} at 4.13\% FPR --- 4.11\,pp above RF
with 6$\times$ lower FPR.
On the CICIDS2017 random split, the average WNN across 120 seeds reaches
99.28\% F1 (0.46\,pp below RF's 99.74\%) in a model $5{,}000\times$
smaller; the best individual genome closes this to \textbf{99.52\% F1}
(0.22\,pp gap), with 0.13\% FPR nearly matching RF's 0.08\%.

On CIC-IoT-2023 (1.3M random 80/20, top-20 features), the average WNN
trails Random Forest in F1 (80.07\% vs.\ 85.53\%), but its false-positive
rate is \textbf{5.8$\times$ lower} (4.32\% vs.\ 25.18\%). The best
individual genome reaches 81.00\% F1 while maintaining 4.68\% FPR ---
still 5.4$\times$ lower than RF. Under the fixed 0.5 threshold (the
fairest comparison to RF/XGBoost \texttt{predict()}), the best genome
achieves \textbf{82.67\% F1}, narrowing the gap to 2.9\,pp.
In a deployed IDS, each false positive generates an alert requiring human
investigation; 25\% FPR is operationally unusable at scale, while
4--5\% is deployable. The WNN achieves this operating point in 100\,KB
on a \$20 FPGA (Table~\ref{tab:fpga}), versus ${\sim}$50\,MB for the
RF model.

\begin{table}[ht!]
\centering
\caption{Comparison with baselines. ``Ours'' rows report mean $\pm$ std
across $N$ seeds (train\_cal threshold); $^\dagger$fixed\_05 for direct
comparison with RF/XGBoost; $^\ddagger$best single genome across all seeds.
RF/XGBoost use raw numeric features; WNN uses thermometer-encoded features.}
\label{tab:baselines}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrl}
\toprule
Method & Dataset / Split & F1 (\%) & FPR (\%) & Acc (\%) & Notes \\
\midrule
\textbf{Ours} & UNSW / temporal & \textbf{87.02} & \textbf{13.76} & 87.16 & 101 runs \\
\textbf{Ours}$^\ddagger$ & UNSW / temporal & \textbf{90.52} & 4.13 & 90.54 & best genome \\
Random Forest  & UNSW / temporal & 86.41 & 25.22 & 86.94 & Same split, top-20 \\
XGBoost        & UNSW / temporal & 85.62 & 27.29 & 86.24 & Same split, top-20 \\
\midrule
\textbf{Ours} & CICIDS / random & 99.28 & 0.24 & 99.55 & 120 runs \\
\textbf{Ours}$^\ddagger$ & CICIDS / random & \textbf{99.52} & 0.13 & \textbf{99.70} & best genome \\
Random Forest  & CICIDS / random & 99.74 & 0.08 & 99.84 & Same split \\
XGBoost        & CICIDS / random & 99.65 & 0.12 & 99.78 & Same split \\
\midrule
\textbf{Ours} & CIC-IoT / random & 80.08 & \textbf{4.26} & 86.85 & 112 runs \\
\textbf{Ours}$^\ddagger$ & CIC-IoT / random & 81.00 & \textbf{4.68} & 87.68 & best genome \\
\textbf{Ours}$^\dagger$ & CIC-IoT / random & 81.65 & 9.01 & 88.58 & fixed\_05 \\
\textbf{Ours}$^{\dagger\ddagger}$ & CIC-IoT / random & 82.45 & 16.98 & 90.07 & best, fixed\_05 \\
Random Forest  & CIC-IoT / random & 85.53 & 25.18 & 92.71 & Same split, top-20 \\
XGBoost        & CIC-IoT / random & 84.13 & 28.34 & 92.07 & Same split, top-20 \\
\midrule
FWIW~\cite{susskind2023fwiw} & UNSW / random$^*$ & --- & 1.57 & 98.50 & 192\,B model \\
\bottomrule
\multicolumn{6}{l}{\small $^*$90/10 deduplicated, oversampled; reports accuracy only, not F1.}
\end{tabular}}
\end{table}

\subsection{Model Size Analysis}
\label{sec:model-size}

WNN model size is determined by the sparse memory footprint: only
addresses visited during training store non-default cell values.
Table~\ref{tab:model-size} compares deployed model sizes.

Due to thermometer encoding correlation, our 34-bit address neurons
contain only ${\sim}$3{,}500 trained entries each (Section~\ref{sec:discussion}),
not the $2^{34}$ that a dense representation would require.  The
11-neuron CICIDS genome stores 41{,}215 entries in 362\,KB of sparse
(key, value) pairs, while the 232-neuron average genome requires
${\sim}$2\,MB.  These are larger than FWIW's 192-byte Bloom filter
model but orders of magnitude smaller than tree ensembles.

\begin{table}[ht!]
\centering
\caption{Deployed model sizes.}
\label{tab:model-size}
\begin{tabular}{lrrl}
\toprule
Model & Neurons & Model Size & Format \\
\midrule
Ours (11n, best)    & 11  & 362\,KB  & Sparse (key, value) \\
Ours (232n, avg)    & 232 & ${\sim}$2\,MB & Sparse (key, value) \\
FWIW                & ${\sim}$30 & 192\,B & Bloom filters \\
Random Forest       & ---  & ${\sim}$50\,MB & Serialized trees \\
XGBoost             & ---  & ${\sim}$10\,MB & Serialized trees \\
CNN-BiLSTM          & ---  & ${\sim}$5\,MB  & Neural weights \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\section{Discussion}
\label{sec:discussion}

\subsection{Connectivity as Feature Selection}

Each neuron's address bits are drawn randomly from specific positions in
the 320-bit (CICIDS, 16-bit thermometer) or 152-bit (UNSW, 8-bit
thermometer) input vector at initialization.  The connectivity remains
fixed throughout training; the GA optimizes only neuron \emph{count},
selecting which neurons to keep or discard.  This implicitly performs
feature selection: neurons whose random connectivity happens to capture
discriminative input combinations survive, while those with uninformative
connections are pruned.

Analysis of our best CICIDS genome (F973, 11 neurons) reveals that each
neuron's 34 address bits span 15--18 of the 20 input features, selecting
1--2 bits from each.  This scattered connectivity contrasts with FWIW's
random permutation, where each filter reads a contiguous slice of the
input.  Despite being randomly initialized, the surviving neurons
collectively cover the most informative feature combinations ---
the GA acts as a neuron-level selection mechanism rather than a
connectivity optimizer.

The grid search phase establishes a strong baseline by exhaustively
testing neuron-count and address-width combinations; the GA then
prunes within the best configuration, reducing neuron count by 35\%
while maintaining or improving accuracy
(Table~\ref{tab:phase}).

\subsection{Threshold Sensitivity}

The gap between validation (val\_cal) and deployable (train\_cal) thresholds
represents the central practical challenge for WNN-based IDS.  On
UNSW-NB15 temporal, this gap is 0.33 percentage points in F1
(87.34\% vs.\ 87.02\%), driven by distribution shift between the
balanced validation set and the temporally distinct test set.  On
CICIDS2017 random, the gap shrinks to 0.03 points (99.40\% vs.\ 99.37\%),
confirming that random splits minimize calibration difficulty.

Interestingly, the simple fixed threshold of 0.5 outperforms train
calibration on UNSW-NB15 (84.25\% vs.\ 82.87\% F1), suggesting that
calibration on the training distribution can overfit when the test
distribution differs substantially.  This finding has practical
implications: for temporal deployment scenarios, a fixed threshold may
be preferable to calibrated thresholds, unless a representative
calibration set from the deployment distribution is available.

\subsection{Encoding Resolution and Saturation}

To determine the optimal thermometer encoding width, we swept from 8 to
64 bits per feature on CICIDS2017 (random split, 2 runs per configuration)
and UNSW-NB15 (temporal split, 2 runs per configuration).
Figure~\ref{fig:therm_sweep} shows the results.

On CICIDS2017, F1-macro increases sharply from 8-bit (99.25\%) to 16-bit
(99.43\%), then plateaus through 64-bit (99.41\%).  The 16-bit encoding
also yields the fewest neurons (avg.\ 58 vs.\ 223 at 8-bit) and the
fastest training (0.9\,h vs.\ 1.5\,h at 8-bit), suggesting that 16
bits provides sufficient resolution for the GA to find compact, accurate
genomes.  The remaining 0.46\% gap to Random Forest (99.74\%) persists
regardless of encoding width, confirming that precision alone does not
explain the difference.

On UNSW-NB15, no clear inflection point emerges: F1 ranges from 82\% to
90\% across all encoding widths with 64-bit showing the most consistent
results (89.56\%).  The high variance (only 2 runs per configuration)
obscures any trend, and the harder temporal split amplifies seed
sensitivity.

\paragraph{Address-tap saturation on CIC-IoT-2023.}
The best 32-bit and 34-bit peak genomes on the 46M dataset
(245n $\times$ 32b vs 500n $\times$ 34b) achieve F1 84.79\% vs
84.73\% --- a 0.06-point gap, well within seed noise. The
4$\times$ larger address space yields no improvement, confirming
that thermometer-encoding bit correlation caps effective
discrimination: with 8-bit thermometer, each feature contributes
only $\log_2(9) \approx 3.17$ effective bits, so a 32-bit address
tap on 20 features already samples roughly half the available
discriminative information.

To test whether this saturation is bounded by the 8-bit thermometer
width, we re-trained both peak architectures at 16/32/64-bit
thermometer encodings on 46M (2 seeds per cell,
Table~\ref{tab:thermo-sweep}).  Neither architecture shows
meaningful F1 movement: the entire sweep lands within $\pm$0.5\,pp
of the 8-bit baseline, well inside seed noise.  This rules out
both interpretations of the saturation: neither wider address taps
nor higher-resolution encoding unlocks additional F1.

\begin{table}[ht!]
\centering
\caption{46M thermometer encoding sweep on two peak architectures
(train\_cal threshold, best-fitness genome). The 8-bit columns are the
baselines from Table~\ref{tab:ciot-46m}; 16/32/64-bit cells report
mean $\pm$ std across 2 seeds.}
\label{tab:thermo-sweep}
\begin{tabular}{lcccc}
\toprule
\multirow{2}{*}{Architecture} & \multicolumn{4}{c}{Thermometer width} \\
\cmidrule(lr){2-5}
 & 8-bit & 16-bit & 32-bit & 64-bit \\
\midrule
\multicolumn{5}{l}{\textit{F1-macro (\%)}} \\
96n $\times$ 32b  & 84.36 & 84.13 $\pm$ 0.08 & 84.53 $\pm$ 0.25 & 84.59 $\pm$ 0.13 \\
500n $\times$ 34b & 84.73 & 84.43 $\pm$ 0.21 & 84.46 $\pm$ 0.41 & 84.87 $\pm$ 0.03 \\
\midrule
\multicolumn{5}{l}{\textit{FPR (\%)}} \\
96n $\times$ 32b  & 1.61  & 1.17 $\pm$ 0.22  & 1.50 $\pm$ 0.27  & 1.69 $\pm$ 0.17  \\
500n $\times$ 34b & 1.91  & 1.51 $\pm$ 0.05  & 1.37 $\pm$ 0.18  & 1.75 $\pm$ 0.02  \\
\midrule
\multicolumn{5}{l}{\textit{Accuracy (\%)}} \\
96n $\times$ 32b  & 97.59 & 97.53 $\pm$ 0.02 & 97.63 $\pm$ 0.06 & 97.64 $\pm$ 0.03 \\
500n $\times$ 34b & 97.68 & 97.60 $\pm$ 0.05 & 97.61 $\pm$ 0.09 & 97.70 $\pm$ 0.01 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Scale-dependent 2-bit finding.}
A downward sweep (2/4-bit) on the same architectures revealed an
unexpected lift at 2-bit on 46M (+1.26\,pp at 96n $\times$ 32b,
+1.04\,pp at 400n $\times$ 8b).  However, a 4-seed validation at
the 1.3M scale showed a balanced-fitness drop of 0.54\,pp,
outside the $\pm$0.3\,pp noise floor.  The lift appears to be
training-set-size-dependent: narrower encodings benefit from the
examples-per-address density that 46M provides but 1.3M does not.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/thermometer_sweep.pdf}
\caption{Effect of thermometer encoding resolution on classification
performance.  (a)~CICIDS2017 F1-macro shows a clear knee at 16 bits,
with diminishing returns beyond.  (b)~The GA finds the fewest neurons
at 16 bits, suggesting optimal information density.
(c)~UNSW-NB15 temporal split shows no clear trend (high variance from
only 2 runs per configuration).  (d)~CICIDS2017 false positive rate
is lowest around 16--26 bits.}
\label{fig:therm_sweep}
\end{figure}

\subsection{FPGA Deployment}

RAM neurons map naturally to FPGA resources: each neuron's lookup table
stores in block RAM (BRAM) or distributed LUTs with the evolved
connection map implemented as compile-time wire assignments (pure routing,
zero logic).  We analyze two deployment configurations targeting the
Xilinx Zynq Z-7020\footnote{The XC7Z020-CLG400 is available on
surplus EBAZ4206 boards (decommissioned Bitcoin miner control boards)
for under \$25; purpose-built development boards (Arty~Z7, ALINX AX7020)
range from \$200--350.  Our synthesis targets the chip, not a specific
board; utilization numbers are identical on any Z-7020 platform.}
(53,200 LUTs, 140 BRAM36 blocks, 612\,KB).

\paragraph{Dense configuration (8--12-bit addresses).}
With address width capped, each neuron's full lookup table is synthesized
into distributed LUTs (Vivado infers zero BRAMs), enabling $O(1)$
direct-lookup inference per neuron.

On CICIDS2017, capping at 12 bits yields 98.53\% F1.
On UNSW-NB15, performance is remarkably stable across address widths:
8-bit genomes achieve 93.5\% F1 in just 6\,KB, nearly matching uncapped
accuracy (Table~\ref{tab:fpga}).

Our best dense genome (104 neurons, 12-bit) synthesizes to just
\textbf{82~LUTs} --- 3.3$\times$ smaller than FWIW's 269~LUTs ---
with \textbf{11.0\,ns} latency ($O(1)$, single cycle at 90.5\,MHz),
matching FWIW's 10.8\,ns.

\paragraph{Sparse configuration (34-bit addresses).}
With the full 34-bit address space, a naive dense table would require
$2^{34} \times 2 = 32$\,Gbit per neuron.  However, thermometer encoding
creates massive address collisions: because each bit within a feature is
correlated (bit $k$ is set iff the feature value exceeds threshold $k$),
a neuron addressing 34 bits across 15--18 features observes only
${\sim}3{,}500$ unique addresses from 2.26 million training examples, not
$2^{34}$.  We store only these trained entries as sorted (key, value) pairs
in BRAM with binary-search lookup.

Our best 34-bit genome (11 neurons, 99.59\% F1, 0.12\% FPR) contains
only 41{,}215 sparse entries (362\,KB).  Vivado synthesis inferred
distributed LUTs rather than block RAM for all configurations
(Table~\ref{tab:fpga}).  Binary search over ${\sim}5{,}000$ entries
per neuron completes in 13 cycles ($O(\log n)$); with pipelining,
throughput remains one classification per cycle at 210\,ns latency,
well above the 12.5\,MS/s needed for 100\,Gbps line-rate inspection.
Post-synthesis estimates; place-and-route typically adds less than
10\% overhead.

\paragraph{CIC-IoT-2023 dense configuration.}
The 200n $\times$ 4b configuration (3.1\,KB total ROM) synthesizes to
\textbf{154 LUTs} (0.29\%), 8 flip-flops, zero BRAMs/DSPs, with
single-cycle latency of \textbf{12.7\,ns} at $F_\text{max}{=}78$\,MHz.
Scaling to the 46M Pareto peak (400n $\times$ 8b, 100\,KB) uses
304~LUTs (0.57\%) at 67\,MHz / 15.0\,ns --- both fit on the same
\$20 Zynq-7020 alongside the UNSW and CICIDS designs.

\paragraph{Comparison with FWIW.}
Table~\ref{tab:fpga} summarizes the resource--accuracy trade-off across
the three FPGA-synthesized datasets (UNSW-NB15, CICIDS2017, and
CIC-IoT-2023). FWIW achieves the smallest footprint (192 bytes, 269 LUTs)
through Bloom filter neurons with random connections.  Our evolved
connections require more resources but deliver lower false-positive rates
and comparable or higher accuracy.  On CICIDS2017, our 12-bit dense
configuration matches FWIW's accuracy class with 3$\times$ lower FPR
(0.50\% vs.\ 1.57\%), while the sparse configuration achieves 99.59\% F1.
On UNSW-NB15, an 8-bit genome with just 20 neurons (1.3\,KB) achieves
93.11\% F1, fitting on the smallest FPGAs. On CIC-IoT-2023, the
4-bit dense configuration (200 neurons, 3.1\,KB) consumes only 154
LUTs --- still 1.7$\times$ smaller than FWIW's 269 LUTs --- and
achieves the lowest FPR (1.96\%) of any IDS WNN reported on this
benchmark, at the cost of a more challenging classification task (the
benchmark mixes 33 attack categories with widely varying signatures).
Scaling to the peak small-memory Pareto point on the full 46M dataset
(400 neurons, 8-bit dense, 100\,KB) raises F1 to 83.13\% and accuracy
to 97.33\% while still using only 304 LUTs (0.57\% of the Z-7020),
at the cost of a modestly longer critical path (15.0\,ns vs.\ 12.7\,ns).
Both CIC-IoT configurations fit comfortably alongside the UNSW and
CICIDS designs on the same \$20 Zynq-7020 part.

\begin{table}[ht!]
\centering
\caption{FPGA synthesis results on Xilinx Zynq Z-7020 (53,200 LUTs, 106,400 FFs).
All designs synthesized with Vivado 2025.2 in out-of-context mode.
Sparse neuron tables are inferred as distributed LUTs (0 BRAMs, 0 DSPs in all cases).
Latency computed from binary-search depth and post-synthesis Fmax.}
\label{tab:fpga}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrrrr}
\toprule
Dataset (split) & Config & F1 & FPR & Acc & $N$ & LUTs (\%) & Fmax & Latency \\
\midrule
\multirow{4}{*}{\shortstack[l]{CICIDS2017\\(random)}}
 & 104n (12b dense)$^\S$ & 98.53\% & 0.50\%          & 99.07\% & 104 & \textbf{82 (0.15\%)} & 91\,MHz  & \textbf{11\,ns} \\
 & 11n (best F1)  & \textbf{99.59\%} & \textbf{0.12\%} & 99.74\% & 11  & 622 (1.2\%)     & 195\,MHz & 210\,ns \\
 & 91n (mid)      & 99.54\%          & 0.12\%          & 99.71\% & 91  & 2{,}907 (5.5\%)  & 94\,MHz  & 436\,ns \\
 & 500n (large)   & 99.23\%          & 0.27\%          & 99.51\% & 500 & 11{,}818 (22\%)  & 59\,MHz  & 644\,ns \\
\midrule
\multirow{3}{*}{\shortstack[l]{UNSW-NB15\\(temporal)}}
 & 92n (small)    & 88.18\%          & 8.87\%          & 88.23\% & 92  & 1{,}939 (3.6\%)  & 92\,MHz  & 413\,ns \\
 & 369n (best F1) & \textbf{90.82\%} & \textbf{4.66\%} & 90.84\% & 369 & 7{,}090 (13\%)   & 61\,MHz  & 574\,ns \\
 & 500n (large)   & 86.81\%          & 8.98\%          & 86.84\% & 500 & 33{,}086 (62\%)  & 64\,MHz  & 547\,ns \\
\midrule
\shortstack[l]{UNSW-NB15\\(random)} & 83n (12b dense)$^\S$ & 93.69\% & 0.63\% & \textbf{99.04\%} & 83 & \textbf{59 (0.11\%)} & 94\,MHz & \textbf{10.7\,ns} \\
\midrule
\multirow{2}{*}{\shortstack[l]{CIC-IoT-2023\\(random)}}
 & 200n (4b dense)$^{\S\dag}$  & 73.34\% & 1.96\% & 80.30\% & 200 & 154 (0.29\%) & 78\,MHz & 12.7\,ns \\
 & 400n (8b dense)$^{\S\ddag}$ & \textbf{83.13\%} & 2.06\% & \textbf{97.33\%} & 400 & 304 (0.57\%) & 67\,MHz & 15.0\,ns \\
\midrule
\shortstack[l]{UNSW-NB15\\(random$^*$)} & FWIW~\cite{susskind2023fwiw} & --- & 1.57\% & 98.50\% & ${\sim}$30 & 269 & 740\,MHz & 10.8\,ns \\
\bottomrule
\multicolumn{9}{l}{\small $^\S$Dense $O(1)$ lookup; all others use $O(\log n)$ sparse search.}\\
\multicolumn{9}{l}{\small $^*$90/10, deduplicated, oversampled. $N$\,=\,neurons. 0 BRAMs, 0 DSPs for all designs.}\\
\multicolumn{9}{l}{\small $^\dag$Trained on the 1.3M random subsample. The same architecture trained on the full 46M dataset achieves 82.20\% F1 (Table~\ref{tab:ciot-46m}).}\\
\multicolumn{9}{l}{\small $^\ddag$Trained on the full 46M CIC-IoT-2023 dataset; peak small-memory Pareto point from the 46M sweep (Table~\ref{tab:ciot-46m}).}
\end{tabular}}
\end{table}

\paragraph{Latency and throughput.}
Our dense configuration (12-bit addresses) achieves 10.7\,ns latency
in 59~LUTs --- matching FWIW's 10.8\,ns while using 4.6$\times$ fewer
LUTs.  However, our Fmax (94\,MHz) is significantly lower than FWIW's
740\,MHz due to the wider accumulation tree across 83 neurons; the
similar latency results from our single-cycle lookup versus FWIW's
multi-cycle hashed Bloom filter.  Our sparse configurations (34-bit
addresses) exhibit higher
latency (210--644\,ns) due to $O(\log n)$ binary search (3 cycles per
step, 11--13 steps), but with pipelining throughput ranges from
59\,MS/s (500-neuron) to 195\,MS/s (11-neuron).  For network IDS,
the relevant metric is throughput: 100\,Gbps line-rate inspection
requires only ${\sim}$12.5\,MS/s (at ${\sim}$1{,}000-byte average
packets), well within the capability of all our configurations.
The sub-microsecond latency of both approaches is negligible compared
to feature extraction overhead, which typically dominates the packet
processing pipeline.

\paragraph{Functional verification.}
We verified both sparse and dense RTL implementations against the Python
training model using Vivado xsim 2025.2.  Testbenches feed 1{,}000
stratified test vectors (500 normal, 500 attack) from both CICIDS2017
and UNSW-NB15 test sets through the synthesized classifiers and compare
output scores against Python-computed expected values.  Both
configurations produce \textbf{identical scores for all 1{,}000 vectors}
(100\% match on each dataset), confirming that the FPGA implementations
faithfully reproduce the trained QSR models.

\subsection{Future Directions}

Several directions offer potential for further improvement.
First, extending to \emph{multi-class classification} (one discriminator
per attack category) would exploit the rich multi-class labels available
in UNSW-NB15, CICIDS2017, and CIC-IoT-2023 (10, 14, and 33 attack
categories respectively), enabling attack-type identification beyond
binary detection.

Second, the 5\% F1 gap between validation and train-calibrated
thresholds on the temporal split suggests that \emph{adaptive threshold
calibration} --- online adjustment using deployment-time feedback ---
could substantially improve real-world performance without requiring
a representative calibration set.

Third, our sparse FPGA configuration uses $O(\log n)$ binary search
(210--644\,ns), while our dense configuration already matches FWIW's
latency (10.7 vs.\ 10.8\,ns).  For sparse configurations, a
\emph{hash-based lookup} replacing binary search could reduce latency
to 15--25\,ns, combining the accuracy of large address spaces with
near-$O(1)$ speed.

Fourth, the remaining 0.46\% F1 gap to Random Forest on CICIDS2017
(0.22\% for the best genome) is not explained by encoding precision
(64-bit yields no improvement) and warrants further investigation.  Our framework supports additional
optimization phases beyond the two evaluated here: \emph{GA bits}
(evolving address width per neuron), \emph{GA connections} (evolving
which input bits each neuron reads), \emph{tabu search} variants of
each (TS neurons, TS bits, TS connections), and \emph{Lamarckian
genesis phases} (neurogenesis, synaptogenesis, axonogenesis) that
progressively refine neuron structure.  These phases, combined with
multi-cluster discriminators and hybrid encoding strategies, offer
multiple paths to close the accuracy gap while preserving the
hardware-friendly lookup-based architecture.

Finally, we observed during the PUB50 batch that best-FPR genomes
occasionally converge on radically smaller architectures than the
best-fitness population (e.g., 6 neurons $\times$ 16-bit address
producing 1.09\% FPR on CIC-IoT-2023 under train\_cal), at the cost
of lower F1. These extreme-compression outliers suggest two
complementary \emph{multi-genome ensemble} strategies. (1)~A
\emph{sequential cascade}: an ultra-small pre-filter WNN eliminates
the majority of benign traffic at near-zero FPR, forwarding only
uncertain or attack-likely flows to a larger second-stage
classifier with higher F1.
(2)~A \emph{parallel mixture-of-experts}: multiple heterogeneous
genomes (low-FPR, high-F1, high-accuracy variants) run concurrently
and combine their outputs via weighted voting or learned gating.
Because single-neuron lookup is $O(1)$ and sub-microsecond, both
designs remain within the line-rate budget even with two or three
genomes active simultaneously. A well-chosen pairing should
achieve strictly better F1/FPR/Acc than any single genome in the
population, amortizing per-packet cost across the deployment while
preserving the high-F1 operating point for the flows that need it.

% ============================================================================
\section{Conclusion}
\label{sec:conclusion}

We presented an evolutionary weightless neural network architecture for
network intrusion detection, combining Quad-State RAM neurons with
genetic algorithm optimization of neuron connectivity.  Our approach
contributes three advances over prior WNN-based IDS:

\begin{enumerate}
\item \textbf{Evolved connectivity.}  A two-phase pipeline (grid search +
  GA neuron optimization) discovers compact, accurate genomes.  The GA
  reduces neuron count by 42\% while improving F1 and halving variance
  compared to grid search alone.

\item \textbf{Three-dataset evaluation with ML baselines.}  We evaluate on
  UNSW-NB15, CICIDS2017, and CIC-IoT-2023, comparing directly against
  Random Forest and XGBoost on identical splits.  On UNSW-NB15 (temporal,
  101~runs), our WNN outperforms both baselines in F1 (87.02\% vs.\
  86.41\%) at half the FPR (13.76\% vs.\ 25.22\%), with the best
  genome reaching 90.52\% F1 at 4.13\% FPR.  On CICIDS2017 (random, 120~runs), our best genome reaches
  99.52\% F1, within 0.22\,pp of RF.  On CIC-IoT-2023 (random, 112~runs),
  the WNN trades 5.5\,pp F1 for \textbf{5.9$\times$ lower FPR}
  (4.32\% vs.\ 25.18\%), a critical advantage for operational deployment
  where false-positive volume determines alert-handling cost.

\item \textbf{FPGA deployment.}  All configurations deploy on a \$20
  Zynq~Z-7020 with sub-microsecond latency and throughput exceeding
  100\,Gbps line-rate requirements.  Our dense $O(1)$ lookup
  configurations use as few as 59~LUTs (0.11\%), and the CIC-IoT-2023
  peak genome (400n $\times$ 8b, 83.13\% F1) fits in 304~LUTs at
  67\,MHz in 100\,KB of model storage.
\end{enumerate}

Future work includes multi-class attack categorization, UNSW-NB15
random-split evaluation with balanced fitness weights, adaptive
threshold calibration, and scaling the evolutionary search to the full
46M CIC-IoT-2023 dataset to close the gap with tree ensembles.

% ============================================================================
\ifanonymous\else
\paragraph{Acknowledgments.}
The author thanks Claude (Anthropic, Opus~4 model family) for substantial
assistance throughout this work, including experimental automation,
GPU accelerator debugging, FPGA synthesis scripting, statistical analysis
of multi-seed batches, and iterative manuscript preparation. The
collaboration spanned dataset pipeline development, ML baseline
evaluation, and the discovery of the UNSW-NB15 feature-name mismatch
that had silently reduced prior runs to 13 of 20 intended features.
All experimental results were verified by the author against
the source database. Git commit history records the full collaboration
trail via \texttt{Co-Authored-By} tags.
\fi

\bibliographystyle{splncs04}
\bibliography{references}

% ============================================================================
\appendix
\section{Full Threshold Mode Breakdown}
\label{app:thresholds}

Tables~\ref{tab:unsw-full}--\ref{tab:ciot-full} report all seven
threshold calibration modes for each dataset. The main body
(Tables~\ref{tab:unsw},~\ref{tab:cicids},~\ref{tab:ciot-phase})
shows only the three key modes (train\_cal, fixed\_05, val\_cal).

\begin{table}[ht!]
\centering
\caption{UNSW-NB15 temporal split, all threshold modes (101 runs,
GA~Neurons best-fitness genome, balanced fitness weights).}
\label{tab:unsw-full}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrr}
\toprule
Threshold Mode & F1-macro (\%) & FPR (\%) & Accuracy (\%) \\
\midrule
Train-cal          & 87.02 $\pm$ 2.58 & 13.76 $\pm$ 4.63  & 87.16 $\pm$ 2.53 \\
Fixed 0.5          & 83.99 $\pm$ 7.26 & 17.99 $\pm$ 15.15 & 84.68 $\pm$ 6.17 \\
Platt              & 83.50 $\pm$ 4.58 & 23.31 $\pm$ 15.07 & 84.26 $\pm$ 4.07 \\
Beta               & 82.07 $\pm$ 5.42 & 28.59 $\pm$ 16.80 & 83.18 $\pm$ 4.50 \\
Empirical          & 75.91 $\pm$ 11.29 & 44.24 $\pm$ 20.28 & 78.86 $\pm$ 7.78 \\
Empirical cumul.   & 82.02 $\pm$ 6.09 & 29.36 $\pm$ 16.89 & 83.20 $\pm$ 4.96 \\
Validation         & 87.34 $\pm$ 2.12 &  5.43 $\pm$ 4.02  & 87.37 $\pm$ 2.10 \\
\bottomrule
\end{tabular*}
\end{table}

\begin{table}[ht!]
\centering
\caption{CICIDS2017 random split, all threshold modes (120 runs,
16-bit thermometer, GA~Neurons best-fitness genome).}
\label{tab:cicids-full}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrr}
\toprule
Threshold Mode & F1-macro (\%) & FPR (\%) & Accuracy (\%) \\
\midrule
Train-cal          & 99.28 $\pm$ 0.52 & 0.24 $\pm$ 0.27 & 99.55 $\pm$ 0.33 \\
Fixed 0.5          & 99.09 $\pm$ 0.63 & 0.45 $\pm$ 0.29 & 99.42 $\pm$ 0.40 \\
Platt              & 99.18 $\pm$ 0.52 & 0.34 $\pm$ 0.21 & 99.48 $\pm$ 0.33 \\
Beta               & 99.16 $\pm$ 0.69 & 0.30 $\pm$ 0.19 & 99.47 $\pm$ 0.42 \\
Empirical          & 98.41 $\pm$ 1.31 & 1.14 $\pm$ 1.10 & 98.96 $\pm$ 0.91 \\
Empirical cumul.   & 99.07 $\pm$ 1.52 & 0.26 $\pm$ 0.16 & 99.43 $\pm$ 0.82 \\
Validation         & 99.30 $\pm$ 0.48 & 0.22 $\pm$ 0.12 & 99.56 $\pm$ 0.30 \\
\bottomrule
\end{tabular*}
\end{table}

\begin{table}[ht!]
\centering
\caption{CIC-IoT-2023 random split (1.3M subsample), all threshold
modes (56 runs, 8-bit thermometer, GA~Neurons best-fitness genome).}
\label{tab:ciot-full}
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} rrr}
\toprule
Threshold Mode & F1-macro (\%) & FPR (\%) & Accuracy (\%) \\
\midrule
Train-cal          & 80.06 $\pm$ 0.50 &  4.32 $\pm$ 0.46 & 86.85 $\pm$ 0.44 \\
Fixed 0.5          & 81.60 $\pm$ 0.40 &  9.11 $\pm$ 0.93 & 88.55 $\pm$ 0.34 \\
Platt              & 81.38 $\pm$ 0.52 & 33.12 $\pm$ 3.25 & 90.72 $\pm$ 0.18 \\
Beta               & 81.10 $\pm$ 0.67 & 34.78 $\pm$ 3.55 & 90.71 $\pm$ 0.15 \\
Empirical          & 67.96 $\pm$ 14.22 & 64.87 $\pm$ 24.03 & 88.69 $\pm$ 2.37 \\
Empirical cumul.   & 82.05 $\pm$ 0.81 & 23.03 $\pm$ 8.19 & 90.21 $\pm$ 0.42 \\
Validation         & 79.88 $\pm$ 0.50 &  3.97 $\pm$ 0.52 & 86.65 $\pm$ 0.44 \\
\bottomrule
\end{tabular*}
\end{table}

\end{document}
